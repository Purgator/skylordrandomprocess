configuration: Release

# restore NuGet packages before running MSBuild
before_build:
  - nuget restore
  - dnu restore

# package Web Application projet for Web Deploy
build:
  verbosity: minimal
  publish_wap: true